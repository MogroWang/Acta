# Acta · 行记 更新日志

## v2.4.0（2026-09-12）

### 新增

- **首次启动 OOBE 设置向导**：桌面客户端首次使用（或未找到软件数据文件夹）时，会进入一个五步引导——先选择「软件数据文件夹」，再依次挑选主题与界面字体、启动动画、应用图标，最后以「欢迎使用 Acta」收尾；所有选择实时预览，顶部进度条指示当前步骤，可随时上一步返回修改。完成设置的偏好会在后续每次启动时自动记住，不再重复引导
- **软件数据位置**：新增「软件自身的设置」与「行记数据」分离的存储定位——便携版默认读取软件目录下的 `data` 文件夹；未找到时进入 OOBE 引导用户选择位置（可使用默认位置或自选文件夹），选定后每次启动都会记住。常规设置新增「软件数据位置」分组，显示当前位置并支持随时更改（设置自动迁移到新文件夹）与一键打开文件夹。设置会镜像写入所选文件夹中的 `settings.json`，清除应用缓存后也能自动恢复；网页端与移动端不受影响
- **MWS 浅色 / MWS 深色品牌主题**：以主色 `#FF6666`、副色 `#66CC66` 设计的双主题；浅色按原色使用，深色为可读性将两色提亮（`#FF8080` / `#7FD894`），整体保持一主一副的克制双色调，主题位于外观设置最前方的「品牌主题」位置
- **完整子待办统计**：统计页面的每条待办现在会逐条、完整列出全部子待办（不再截断为单行预览），已完成子待办打勾划线，一眼看清当天的执行明细
- **自定义动画下拉列表**：全部原生下拉（列表筛选、排序、设置项、导出选项等 25 处）替换为统一的自绘菜单——弹层锚定在触发控件上、缩放+淡入展开、沿同一路径收回，支持键盘方向键/回车/Escape 操作、自动上下翻转与视口边界规避，深浅主题与界面字号自动适配
- **自定义滚动条**：移除原生滚动条样式，统一为细圆角滑块 + 透明轨道的自定义滚动条，悬停加深，颜色跟随各主题的中性色，列表、编辑器、设置与各弹层全部生效

### 修复

- **本地客户端更换应用图标失效**：预设图标此前完全依赖网页画布处理，在部分桌面 WebView 环境（CSP、协议图像染色等差异）下画布管线不可用，导致切换图标无效果或报「应用图标应用失败」；现在前端会先把图标经 fetch 转为 data URL 再进画布（规避染色），画布仍失败时自动回退为「空数据 + 预设名」，由 Rust 端用打包内置的同一张预设图应用并持久化，恢复默认图标的行为保持不变
- **启动动画时间倍率标反**：「动画时间」滑杆此前标注为播放倍率，实际却是时长倍率——选 2× 反而更慢、0.5× 反而更快；现改为「动画速度」，数值越大播放越快（2× 即两倍速快放），内部时长换算自动完成，预览与实际启动表现一致
- **预览动画按钮位置重设**：原先孤悬在设置组下方的「预览动画」按钮，改为内嵌到「动画预设」一行的右侧圆形按钮，点选预设即可随手预览，预览时按钮图标旋转提示
- **折叠侧边栏再次展开时元素动画异常**：工作区列宽过渡此前只声明在折叠态，展开时列表/编辑器区域会瞬间跳变；侧边栏内部多处用 `width: auto↔44px` 做过渡（不可插值，只能瞬跳），折叠期隐藏的文字在展开时也会生硬闪现。现在改为：工作区列宽过渡仅在折叠/展开瞬间由 `sidebar-resizing` 类启用（视图切换不受影响）、内部元素全部改用可插值的 max-width 过渡、被收起的文字在展开时以短促淡入回归；顺带清理了 brand 动画区段的整段重复声明

### 变更

- **按钮悬停反馈改为缩放**：设置与界面中约 30 处按钮、卡片的悬停反馈由上下位移（translateY）改为轻微缩放（scale），观感更稳定统一；列表项的横向滑动暗示保持不变
- **统计页背景与主题一致**：统计页此前以待办色调叠加染色，现改用主题标准背景色（`--panel`），与整体界面观感统一
- **重复代码清理**：移除 interface.css 中 logo 光效区段的整段重复规则与重复动画声明
- **网页端缓存与网页版随版本升级**：GitHub Pages 网页版（PWA）同步至 v2.4.0，Service Worker 缓存名升级为 `acta-2.4.0-oobe-mws`；Android 工程内嵌 Web 资源同步更新
- **测试扩展**：冒烟测试新增 OOBE 全流程、自定义下拉菜单、MWS 主题强调色、启动速度语义反转与桌面图标预设兜底的自动化断言

## v2.3.0（2026-09-06）

### 修复

- **桌面端启动闪现默认窗口**：桌面端窗口会在网页内容就绪前提前显示，启动时先看到一闪而过的纯色默认窗口、页面加载后才「重绘」出界面；现在窗口会等到页面完全加载、主题色启动画面绘出后才显示，首帧即是启动画面，不再闪现默认窗口（Rust 侧 3 秒兜底保持不变）
- **启动闪现根因修复（平台配置覆盖）**：上面的修复此前实际未生效——平台专属配置 `tauri.macos.conf.json` / `tauri.windows.conf.json` 的窗口定义会整体替换主配置，「初始隐藏」被覆盖回默认的「立即可见」；现已在两份平台配置中恢复初始隐藏。macOS 窗口进一步改为透明（启用 `macOSPrivateApi`），即使网页首帧尚未合成就显示窗口也完全不可见，加载后约 150ms 即提前放行窗口显示，任何时序下启动都不会再有闪现
- **待办/笔记筛选控件相互遮挡**：「所有待办」的优先级、截止时间、归类筛选框在中栏较窄时会被挤压缩排，「显示已完成」与排序控件叠压在筛选框上方、难以点击；现在筛选区支持换行、筛选组按内容自然占位，窄栏下筛选框与「显示已完成」「排序」自动分行排布，互不遮挡（笔记筛选同样受益）
- **统计时区错位**：统计页此前用 UTC 日期给条目分组，东八区等时区在凌晨 0–8 点创建的待办与笔记会被计入前一天，选「今天」也看不到它们；现在统计筛选、按天分组与清单长图统一按本地日期计算
- **快捷键误触**：在编辑器、速记、设置等输入框中按 ⌘/Ctrl+N 或 ⌘/Ctrl+K 不再误触发「新建笔记」或「聚焦搜索」，按住组合键也不会连续创建
- **关闭窗口丢最后一笔**：保存存在 320ms 防抖且写盘为异步，修改后立即关窗或切后台可能丢失最后一笔编辑；现在页面隐藏或关闭前会立即冲刷待保存内容（本地档案同步落盘，文件夹与网盘档案尽力写入）
- **刷新数据丢弃未保存修改**：点击「刷新数据」前 320ms 内的编辑此前会被防抖定时器取消并被远端快照覆盖；现在会先落盘再加载远端数据
- **统计「已选 N 件」计数不准**：切换时间段或数据档案后，选中计数与实际导出的条目可能不一致；失效的选中项会被自动清理，计数、勾选框与导出长图始终对应同一批条目
- **删除确认弹窗多语言**：删除二级提示此前在英文/繁體界面下固定显示简体中文；现已接入多语言字典，标题、两个选项与底部按钮随界面语言切换
- **回收站关联可恢复**：有关联的待办/笔记移入回收站时双向关联会被立即剪断，恢复后无法找回；现在移入回收站会暂存关联，恢复后自动重建双向链接
- **统计预览截断残缺**：清单预览在转义后的文本上按字数截断，遇 `&` 等符号时可能显示残缺的字符实体；改为先截断原文再转义
- **列表归类色点加固**：列表卡片的归类色点未做转义，同步导入的异常颜色值可能破坏卡片结构；加载时统一校验颜色格式，渲染时转义
- **网页离线缓存污染**：Service Worker 此前会缓存错误响应页与 Range 部分响应，离线回退时可能返回坏页面；现在仅缓存完整成功的响应
- **更新检查输出加固**：「关于」页的版本号与下载链接来自 GitHub API，现已统一转义并校验链接域名后再渲染
- **删除确认连点竞态**：上一个确认对话框关闭动画期间再次打开时，旧的关闭定时器可能错误关闭新弹窗或让旧请求永远悬空；重入时会立即结算旧请求
- **卡片残留「0 标签」徽章**：标签功能下线后，笔记列表卡片仍显示恒为 0 的标签计数徽章，已移除

### 新增

- **启动动画**：新增应用启动画面——Acta 文字 logo 在窗口正中央淡入，启动即以当前主题的界面底色全屏呈现（主题在首帧渲染前的 `<head>` 阶段应用；桌面端窗口初始隐藏，待启动画面绘制完成后才由前端显示窗口，Rust 另有 3 秒兜底，首帧永远是主题色、彻底告别打包版的原生底色闪烁）。页面加载完成后，logo 以流畅的非线性动画飞向标题栏品牌 logo 的确切位置并匹配其大小（macOS 交通灯布局同样自适应），落位后保持常亮，直到与界面一同淡入交接；同时与界面分割线完全契合的强调色线条（桌面端：标题栏下横线、列表两侧竖线；移动端：标题栏下横线、底部栏上缘横线）依次绘出、把背景划分为工作区区块，绘制完成后主界面再随之淡入。启动背景改用界面自身的底色变量，深浅与彩色主题下渐隐交接均不再闪变。开启「减少动态效果」或系统减弱动态时自动跳过；即便页面加载异常缓慢，也会在数秒内自动退出，绝不会挡住界面
- **窗口位置与大小记忆**：桌面端现在会记住上次关闭时的窗口位置、大小与最大化状态，下次启动原位恢复（窗口仍在隐藏阶段即完成恢复，不会看到窗口跳动）；保存的几何会按当前显示器校验，屏幕布局变化后不会把窗口恢复到看不见的位置
- **启动动画设置与更多预设**：外观设置新增「启动画面」分组——可关闭启动动画（关闭后启动直达工作区）、选择动画预设、用 0.5×–2× 滑杆调节整体播放时长，并可随时点「预览动画」重播完整编排；保留原有线条绘制动画为默认预设「Acta 线构」，新增「静谧淡入」（logo 轻盈上浮浮现、无线条、与工作区柔和交叉溶解）与「聚焦缩放」（logo 由模糊到清晰聚焦定格、工作区微微缩放归位）两款预设

### 变更

- **应用图标平台化适配**：Windows 图标（打包 .ico 与安装器/商店资源）加入 18% 圆角；macOS 打包图标维持 26+ 全出血规范；运行时「切换预设图标」按平台同规则处理（macOS 26+ 铺满画布、交由系统自动切圆角，旧版 macOS 保留居中留边，Windows 烘焙同规格圆角），默认预设源图重新生成为全不透明、消除边缘半透明光晕；新增 `scripts/generate-desktop-icons.mjs`，从 app-icon.png 一键重新生成全套桌面图标
- **网页端缓存与网页版随版本升级**：GitHub Pages 网页版（PWA）同步至 v2.3.0；Service Worker 缓存名升级为 `acta-2.3.0-splash-settings` 并把启动动画脚本纳入预缓存，旧缓存自动清除
- **笔记正文消毒**：引入 DOMPurify（随应用打包，离线可用），从同步、导入或分享数据加载的笔记 HTML 一律先做白名单消毒再渲染，纵深防御脚本注入
- **CSP 收紧**：内容安全策略补充 `base-uri 'self'`、`form-action 'self'`、`object-src 'none'`，并为桌面端保留的内联清理脚本加入哈希白名单
- **死代码清理**：移除指向已下线标签编辑器的残留监听器等无效代码

## v2.2.0（2026-09-05）

### 新增

- **回收站**：删除的待办与笔记会进入回收站；回收站特意不会自动倾倒，内容也不计入收集箱、日历、统计与归类计数，欢迎随时回来翻一翻。支持单条恢复、彻底删除，也可一键清空（需确认）。侧栏新增「回收站」入口并显示件数，移动端底部导航同样可达
- **删除二级提示**：点击删除待办或笔记时弹出应用内确认对话框，可选「移入回收站」（默认，可随时恢复）或「直接删除」（立即彻底删除），防止误操作；桌面端不再依赖不支持的浏览器原生 `confirm`
- **统计页面重构为待办笔记清单**：移除原有的数量总览、完成率环形图、30 天柱状图与优先级/归类分布等统计板块，改为按时间段收集待办与笔记的清单。支持全部时间、今天、最近 7/30/90 天及自定义起止日期筛选，按天分组展示；勾选任意条目后可一键生成精美的清单长图（跟随当前主题配色，桌面端弹出保存对话框，移动端写入相册所选目录，网页端直接下载）
- **更多面板快捷操作**：待办与笔记的「更多」面板新增快捷操作区块——待办可标记完成/重新打开、复制标题、复制补充说明、在日历中查看、移入回收站；笔记可复制标题、复制全文、复制 Markdown、移入回收站

### 变更

- **未设置的时间不再显示**：待办编辑器顶部与更多面板中的开始、截止时间在未设置时直接隐藏，不再显示「未设置」占位
- **更多面板排版**：快捷操作以双列按钮网格呈现，危险操作（移入回收站）单独标红，与时间安排、优先级、归类卡片并列
- **新界面风格统一**：删除确认对话框、回收站与统计清单页对齐既有设计规范——对话框采用与速记一致的红调头部、右上角关闭按钮、标准页脚与开合动画；回收站卡片与统计清单改用实线卡片、悬停位移与统一的入场动画；危险操作色统一为应用的删除红；深色与发光主题自动适配

### 修复

- **补充说明换行丢失**：点击「添加子任务」等操作重建编辑器后，待办补充说明中的换行不再被重置；补充说明现按所见即所得的方式保留换行，速记中粘贴的多行说明也能正确显示
- **统计页空时间段控件消失**：当所选时间段（如今天）没有内容时，统计页的时间段选择器等控件不再消失，可以随时切换回其他时间段
- **视图切换布局残留**：修复从统计、回收站或日历切回列表视图时，中栏与编辑栏宽度偶发停留在切换前状态的问题；全宽视图切换现在立即到位
- **冒烟测试平台适配**：Tauri 桥接测试页补齐 Windows UA 模拟，修复在 macOS 主机上因平台检测导致的测试失败

## v2.1.0（2026-08-31）

### 新增

- **统计页面**：原「已完成」页面升级为「统计」，统计当前档案全部笔记与待办：数量总览卡片、待办完成率环形图、最近 30 天新建记录柱状图、优先级分布与归类分布横向条形图
- **显示已完成待办**：「所有待办」页面新增「显示已完成」勾选框（移动端为头部开关），已完成的待办可以直接在待办列表中查看
- **正文行间距设置**：笔记编辑器设置新增「正文行间距」选项（1.4–2.0），同时作用于可视化编辑器与 Markdown 源码
- **移动端统计入口**：移动端顶栏新增统计按钮，可在任意视图快速打开统计页

### 变更

- **主题顺序与命名**：「纸色护眼」移动到主题列表第一位；「深海霓虹」更名为「深夜霓虹」，以紫色与青色为主色调，霓虹发光更明显并带有灯控间歇性闪烁，副标题为「特殊主题」
- **主题内容色回归**：黑白浅色、黑白深色、纸色护眼三个主题恢复待办（浅蓝）、笔记（浅黄）、日历（浅绿）主题色，深色主题按可读性适配
- **默认行距调整**：笔记正文默认行间距从 1.85 调整为更自然的 1.6
- **macOS 标题栏**：logo 移至窗口右上角，避开红绿灯控件；版本号显示在 logo 左侧
- **macOS 应用图标**：按 macOS 26+ 规范重新生成图标集（1024×1024 全出血、完全不透明源图，由系统自动应用圆角遮罩），修复旧图标边缘半透明光晕与新系统不兼容的问题

### 修复

- **归类添加按钮失效**：桌面端（Tauri）不支持浏览器原生 `prompt`，新增归类改用应用内命名对话框
- **撤销动画符号显示异常**：撤销待办完成时的「↶」符号在 macOS 上渲染异常，改用 SVG 图标绘制
- **文本框焦点外边框**：Markdown 编辑器（源码与可视化）和补充说明获得焦点时不再显示强调外边框

## v2.0.0（2026-08-29）

这一版对界面做了一次全面的视觉焕新：全新的纸张主题、独立的展示字体排版，以及更一致的阴影与动效，并新增列表排序方式。

### 新增

- **列表排序方式**：笔记与待办列表可按最近更新、最近创建、截止时间、优先级或标题排序，所选方式会被记住
- **纸色护眼主题**：原「黄色护眼」重新设计为纸张质感的全新主题，并成为新的默认主题
- **GitHub Actions 构建**：推送 `v*` 标签后自动构建 Windows x64 便携版 exe 与 macOS（Apple 芯片）DMG
- **Windows 本地构建脚本**：新增 `build-tauri-windows.ps1`，工具链根目录与 VsDevCmd 路径可通过参数或 `ACTA_TOOLCHAIN_ROOT` / `ACTA_VSDEVCMD` 环境变量覆盖

### 变更

- **界面视觉焕新**：重塑阴影层级与动效缓动曲线，统一控件间距与层次，列表面板宽度调整为 330px
- **展示字体排版**：标题、设置页与日历数字等展示性文字引入独立的展示字体，跟随「界面字体」设置
- **主题色板微调**：黑白浅色、黑白深色、蓝黄、森林晨雾、海盐晚霞、糖果气泡、深海霓虹、极光夜色全部主题重新调色
- **PWA 缓存更新**：缓存名称变更，PWA 会自动失效旧缓存并加载新版本资源

### 修复

- **桌面端旧版本资源残留**：启动时清理遗留的 Service Worker 与缓存，修复新旧版本共用 WebView 数据目录时，新版启动后仍显示旧界面的问题（用户数据 localStorage / IndexedDB 不受影响）
- **Android 15 边到边显示**：修复强制 edge-to-edge 时系统栏后方条带颜色不随主题变化的问题
- **macOS Dock 图标恢复**：读取持久化图标后立即恢复 Dock 图标，不再依赖主窗口已存在

## v1.2.0（2026-08-01）

### 行记数据 重要更新

- 行记数据支持导入现有档案。
- 行记数据新建空白档案时，若用户选择非空文件夹，则提醒用户是否确认选择，否则将清空文件夹中文件。

### 笔记导出 升级

- Markdown 导出 UI 优化（旧版 1.1.000 UI 存在 bug）。
- PDF 导出支持图片型或文字型（旧版 1.1.000 仅支持图片型）。
- PDF 导出文字型时需下载字体（Noto Sans SC），下载后用户也可选择删除。
- 图片导出支持选择 JPG 或 PNG 格式，以及更多比例、分辨率等选项。
- PDF 与图片导出全面优化 UI，支持实时预览导出样张（旧版 1.1.000 为模拟预览，非实时）。

### 关于页 小更新

- 支持对比 GitHub Releases 的最新版本以检查更新。

### macOS

- 本版本已构建 macOS arm64 版本（.app），可拖入 application 文件夹，即开即用。
- 为 macOS 单独适配，顶栏 LOGO 移至右上角，为红绿灯按钮让位置。

### Windows

- 解决 Windows 版本运行 1.1.000 旧版后再启动 1.2.0 新版无法正常使用的严重问题。

### 版本号

- 遵循 SemVer 规则，本版本命名为 1.2.0。

### 发布链接

- Windows：<https://wwaso.lanzouu.com/iiBd4407emha>（密码：1bcw）
- Android：<https://wwaso.lanzouu.com/iaBXD407elzc>（密码：7b1f）
- macOS：<https://wwaso.lanzouu.com/idMHY407elqd>

## v1.1.000（2026-07-27）

- 优化移动端日历月视图：点击待办图标可跳转至周视图对应日期，待办图标横向排列并避免溢出框外。
- 日历现在会显示当天创建的笔记了。
- 优化移动端收集箱页面的归类入口调整至右上操作按钮下方，归类列表支持进入对应归类的编辑页面。
- 统一化收集箱、待办与笔记页面的筛选功能与交互。
- 优化移动端归类管理 UI 界面：更适合小屏操作的横向归类列表、单列编辑区、触控按钮与不溢出布局。
- 移除设置中的"开发者测试"页面及通知测试功能。
- 调整左上角版本号显示位置。
- 增强待办时间显示：在列表中的待办优先显示开始至截止时间；未设置时仅显示待办创建时间。待办详情同时显示创建、开始与截止时间，并在修改后会即时更新。
- 优化 Windows 版本顶栏收缩时未跟随收缩的问题。

### 发布链接

- Windows 构建：<https://wwaso.lanzouu.com/iSnuT3z26pvi>（密码：f1x9）
- Android 构建：<https://wwaso.lanzouu.com/iNfXa3z26q1e>（密码：5sat）

## v1.0.008 DEV（2026-07-26）

### 桌面端与应用图标

- 桌面框架由 Electron 迁移至 Tauri，大幅减少内存占用。支持 Windows（正式构建版本即将发布）/ macOS（仍需测试，延迟构建）。
- 重新适配文件导入导出、同步目录、WebDAV、缓存清理和自定义应用图标。
- 清理 Electron 相关文件，进一步缩小 Windows 版本体积。
- 更新四套应用内置图标，用户可点击更换（仅本地客户端）：默认书页、正·书页、勾勒·书页、初版简洁。
- 自定义图标仅影响 Tauri 桌面端和 Capacitor 移动端，不修改网页标题图标。
- 修复 Tauri 打包后自定义图标失效的问题，图标可持久化并在下次启动时恢复。
- 修复 Tauri 打包后的 Windows 构建版本启动时出现命令行窗口的问题。
- 构建目标从 x86 的安装版本切换到 x64 免安装版本，Windows 用户可一键运行应用。

### 笔记与待办

- 笔记新增创建日期、最后编辑时间显示。
- 待办移除"持续时间"，新增不可修改的创建时间和用户自定义的开始时间和截止时间。
- 待办的开始时间默认使用创建时间，截止时间由用户自行设置。
- 支持取消待办开始或截止时间，并提示该待办将不在日历中显示。
- 日历中，跨天待办会显示在经过的每一天。
- 新建待办默认为"未归类"。
- 笔记和待办的更多设置中，归类改为仅选择框，不再提供编辑名称功能。
- 添加更多设置窗口关闭时的非线性流畅动画。

### 日历与移动端

- 优化移动端月视图布局，待办使用紧凑图标显示。
- 月视图不再显示待办时间；非移动端保留标题显示。
- 恢复周视图原有内容显示。
- 移动端周视图支持整个卡片区域流畅滑动。
- 移动端年、月、周视图采用分级点击逻辑：月份 → 月视图、周区域 → 周视图、日期 → 日视图。
- 从月视图进入周视图时，会自动定位到用户选择的日期。
- PC 端日历点击逻辑暂时保持不变。

### 界面与问题修复

- 修复侧边栏折叠后的图标为圆形图标。
- 修复 PC 端速记页面无法完整显示的问题。
- 优化移动端日历点击区域和周视图滑动流畅度。
- 完成 Tauri 桥接、速记、数据档案、日历、归类及 Markdown 导入导出的综合冒烟测试。

## v1.0.007 DEV（2026-07-24）

本次更新重点增强了笔记编辑与导出体验。

- 全面增强笔记工具栏，新增删除线、高亮、行内代码、有序列表、任务列表、引用、代码块、分隔线、链接管理及撤销/重做等功能。
- 新增笔记沉浸编辑模式，并优化进入、退出动画及移动端布局。
- 增强 Markdown 源码与可视化编辑模式的切换和格式处理。
- 笔记导出新增 PDF 和 PNG 图片格式，并保留 Markdown 源文件导出选项。
- PDF 导出支持纸张尺寸、方向、页边距、字号及标题、日期显示设置。
- 图片支持长图或分页导出，并可自定义比例、主题、背景图片、遮罩、水印、字体及文字间距。
- 设置中新增"笔记编辑器"页面，可调整各级标题字号与样式、工具栏位置以及工具名称显示。
- 归类管理新增 Emoji 选择器，改善自定义简称与移动端归类入口。
- 优化全局字体缩放，使列表、编辑器、设置和其他界面的字号变化更加统一。
- 优化笔记标题自适应、编辑器滚动定位、修改时间显示和状态切换。
- 侧栏新增当前版本标识，并同步更新应用信息与离线缓存。

## v1.0.006 DEV（2026-07-23）

### 日历

- 使用全新的"日历"页面取代原"今天"页面。
- 支持年、月、周、日四种视图。
- 月视图增加周数，采用紧凑型待办展示，内容溢出时自动省略。
- 周视图支持独立滚动条，并针对移动端加入横向滑动和提示。
- 周视图与日视图支持完成或撤销待办及子待办。
- 日历中可查看待办与笔记之间的链接关系。
- 优化视图切换和日期跳转动画，采用更自然的非线性过渡。
- 增大日历字体并将日历主题色调整为绿色。
- 修复日历切换页面后待办或笔记标题显示错误的问题。

### 待办与笔记

- 为待办和笔记列表加入筛选功能。
- 待办新增持续时间设置。
- 改进归类管理，可编辑归类名称、简称、Emoji 和颜色。
- 手机端新增"归类"入口，可查看所有归类及内容数量。
- 增大速记菜单文字，提高移动端可读性。
- 改进笔记 Markdown 编辑、导入、导出和格式往返处理。

### 数据与同步

- 将"网盘同步"更名为"数据同步"，并移动至"行记数据"下方。
- 新增完整的数据档案管理，可创建、复制、切换和连接不同档案。
- 新增数据刷新按钮，启用同步时优先读取同步位置，否则重新读取当前数据档案。
- 统一 Windows、Android 与网页端的数据结构。
- 每条笔记和待办独立保存，减少跨平台同步不匹配的情况。
- 保留旧版数据读取与自动迁移能力。

### 外观与个性化

- 支持分别自定义待办、笔记和日历主题色。
- 增加更细致的界面颜色选项。
- 新增多款彩色主题。
- 新增两款深色模式发光主题。
- 新增应用图标自定义功能，提供多种预设并支持上传图标。
- 改进深色模式下的侧栏、弹窗和控件显示效果。

### 界面调整

- 将刷新、设置和侧栏折叠按钮集中至左下角。
- 将保存状态移动至原折叠按钮区域，并改为左对齐显示。
- 手机顶栏右上角增加归类、刷新数据和设置按钮。
- 改进侧栏折叠后的图标对齐、归类简称和操作菜单。
- 优化设置窗口、归类窗口和关联窗口的动画与移动端布局。
- 修复原生及页面对话框在安全区、软键盘环境下的错位问题。

### Android

- 移除应用启动画面及相关 Splash 资源。
- 修复原生对话框的尺寸、居中和软键盘适配。
- 统一应用、Activity、系统对话框和 Android 12+ 图标资源。
- 修复残留的 Capacitor 默认图标问题。
- Android 构建编号升级至 `versionCode 7`。
- 新版本保持与上一 Android 构建相同的签名，可直接覆盖更新。

### Windows

- 更新 Windows 安装版和便携版。
- 应用文件版本更新为 `1.0.6.0`。

## v1.0.005 DEV（2026-07-22）

### 数据与同步

- 移除会重新生成或同步初始行记数据的旧流程，避免网盘同步时混入默认数据。
- 新增"清除缓存并重新加载"功能。
- 统一 PC、浏览器、WebDAV 和 Android 的数据文件夹结构。
- 待办独立保存为 JSON。
- 笔记拆分为 Markdown 正文和 JSON 配置。
- 支持读取旧数据目录和 `acta-library.json`。
- 只有新版文件全部写入成功后才清理旧格式。
- 调整清单和旧项目文件的写入顺序，降低网盘同步中断造成的数据损坏风险。

### Markdown 编辑器

- 增强 Markdown 与富文本互转。
- 增加 Markdown 源码编辑模式。
- 支持引用、删除线、高亮、行内代码。
- 支持任务列表、分隔线和带语言标记的代码块。
- 完善撤销、重做及不安全链接过滤。

### 速记

- 新建菜单增加红色小闪电图标"速记"选项。
- 用户可使用引导弹窗表格快速创建笔记或待办。
- 速记待办支持添加多个子事件。
- 速记创建完成后不会自动打开项目。

### 归类管理

- 折叠简称改为自定义简称文字或 Emoji。
- 支持留空后自动取字生成简称。
- 放大侧栏折叠状态下的归类图标文字。
- 完善归类管理窗口的紧凑和移动端布局。

### Android

- 修复无法选择本地文件夹的问题。
- 使用 Android SAF 并持久保存文件夹读写权限。
- 修复深浅主题状态栏颜色映射相反的问题。
- 状态栏和导航栏根据实际背景亮度自动选择深色或浅色图标。
- 完成 `1.0.005 DEV` Android Debug 构建和签名校验。

## v1.0.004 DEV（2026-07-19）

### 行记数据档案

- 重新设计"行记数据"设置页的逻辑与界面。
- 支持新建空白数据档案并自定义名称。
- 支持选择档案保存位置。
- 默认保存在软件本地；浏览器环境使用本地缓存并提示容量限制。
- 新增数据档案列表管理。
- 支持编辑档案名称和路径。
- 支持复制、完整导出和删除档案。
- 优化按钮布局及非线性过渡动画。

### 归类管理

- 归类编辑按钮移动至"新建归类"按钮旁。
- 新增独立归类管理窗口。
- 支持集中查看和选择所有归类。
- 支持编辑归类名称、颜色和折叠简称。
- 简称最多三个字符，留空时根据归类名称自动生成。
- 支持查看归类下的笔记和待办列表，并可直接打开项目。
- 新增删除归类功能。
- 重新设计管理窗口尺寸与响应式布局：
  - 桌面端最大约 `1120×780`。
  - 笔记、待办列表独立滚动。
  - 底部操作栏不再遮挡内容。
  - 中等窗口自动压缩侧栏。
  - 移动端使用全屏单列布局。
- 修复通用弹窗样式将窗口错误覆盖为 `480px` 的问题。

### 折叠导航栏

- 修复折叠导航栏整体错位。
- 修复导航图标与按钮边框中心不对齐。
- 折叠后将"新建归类"和"管理归类"合并为一个操作按钮。
- 点击合并后的操作按钮可打开二级操作菜单。
- 放大折叠状态下的归类圆点。
- 圆点中央显示归类简称。
- 修复折叠 Logo 切换时的瞬移。
- 完整 Logo 与迷你 Logo 改用统一中心锚点。
- 修复迷你 Logo 点击动画导致的垂直位移。
- 调整折叠按钮，使其完整位于顶栏分割线右侧并保持固定间距。

### 窗口动画

- 补充部分弹窗缺失的显示动画。
- 补充关闭弹窗时缺失的过渡动画。
- 根据反馈取消不合适的关闭回弹效果。
- 关闭动画改为平滑淡出、缩小和下移。
- 优化归类菜单、设置窗口等界面的非线性动画。

### 语言与国旗图标

- 修复 Windows 将国旗 Emoji 显示为英文字母的问题。
- 改用稳定的彩色国旗图标资源。
- 移除国旗图标描边。
- 调整图标比例，使外观更接近标准国旗。

### 网盘与本地同步

- 将"OneDrive 本地文件夹"重命名为"本地文件夹"。
- 明确该功能是通用本地同步方式，不再局限于 OneDrive。
- 支持本地磁盘、局域网共享目录及其他网络位置。
- 此选项在全部支持的平台上可用。

### Android

- 修复不同主题下 Android 状态栏颜色与应用顶栏不匹配。
- 移除活动主题中的空背景设置。
- 原生启动阶段设置明确的窗口和系统栏背景色。
- 状态栏颜色直接跟随当前主题的侧栏颜色。
- 深色主题自动使用浅色状态栏图标。
- 浅色主题自动使用深色状态栏图标。
- 自定义主题根据颜色亮度自动选择图标明暗。
- 应用从后台恢复时重新同步状态栏。
- 同步修正 Android 导航栏初始颜色。

### Android 包信息修改

- 旧包名：`com.acta.xingji`
- 新包名：`com.mws.acta`

## v1.0.003 DEV（2026-07-18）

- 重新平衡并合并移动端底部导航布局。
- 修复深色模式下桌面左下角文字和元素对比度。
- 彻底删除笔记、待办中的标签，只保留"归类"。
- 归类支持编辑名称和删除。
- 增大笔记、待办字体，并跟随自定义字号。
- "所有笔记"和"所有待办"搜索栏下不再显示筛选器。
- 数据文件改成完整文件夹结构。
- 每则笔记和待办分别保存，不再全部塞入单个 JSON。
- 导入、导出和网盘同步均处理完整数据文件夹。
- 待办应用淡蓝色主题，笔记应用淡黄色主题。
- Markdown 编辑器始终显示外边框。
- 列表摘要根据空间自动省略。
- 左侧功能栏支持带非线性动画的折叠。
- 移动端增加底部安全空间、回弹效果，并移除触摸设备的悬停状态。
- LOGO 动画改为轻量中心粒子，并修复卡顿和蓝色焦点框。
- 修复创建项目时截止时间与创建时间不一致的问题。
- 截止日期支持年月日、时分秒分别编辑，并需勾选确认。
- 优先级名称改为"优先处理、稍后处理、延缓处理"。
- 截止日期、优先级、归类收进三点菜单。
- 修复归类文本框越界、重命名和删除迁移逻辑。
- OneDrive 曾尝试 Microsoft Graph 鉴权，随后完全撤回。
- OneDrive 最终恢复为"选择本地 OneDrive 文件夹，由客户端上传"。
- 新增 OneDrive/WebDAV 同步模式切换。
- WebDAV 支持服务器地址、账号、密码、连接测试、上传、下载和自动同步。
- Windows WebDAV 改走 Electron 原生网络通道；Android 走 Capacitor 原生 HTTP。
- 网页版增加 CORS、混合内容、证书和网络错误诊断。
- 修复 Android 中文输入：
  - 关闭受限的 `captureInput`。
  - 恢复原生 IME/InputConnection。
  - 保护拼音组合输入。
  - 防止选词回车误触发新增子任务或提交名称。

## v1.0.002 DEV（2026-07-17）

### 平台支持

- 添加 PWA 支持。
- 构建 Android 和 Windows 客户端。
- 使用新版应用图标并生成多分辨率资源。
- 通知功能适配网页、Windows 和 Android。
- Android 支持系统返回键和状态栏配色。

### 数据与导航

- 将"标签"和"文件夹"合并为"归类"。
- 将"工作区"统一改名为"行记数据"。
- 新增"已完成"栏目；子步骤全部完成的待办从收集箱移入这里。
- "所有待办"不再混入笔记，"所有笔记"不再混入待办。
- 本地文件夹名称显示为数据空间名称。

### 桌面端

- 顶部栏改为完整横栏，保存状态移动至右上角。
- 中间列表栏支持拖动调整宽度。
- 优化列表选择动画和 LOGO 聚焦动画。
- 修复深色模式字体、背景和新建弹窗。
- 修复待办勾选图形居中。

### 移动端

- 设置按钮移动至右上角。
- 修复点击笔记或待办后残留选中状态。
- 调整底部导航按钮、文字和对齐。
- 调整设置侧栏字号。
- 优化页面底部安全间距。

### 关于页面

- 添加作者"MogroWang Studio"及官方网站链接。
- 添加"由 Codex 驱动创作、开源免费"等说明。

## v1.0.001 DEV（2026-07-17）

### 核心功能

- 添加笔记与待办双向关联。
- 让待办优先级真正参与显示与排序。
- 支持跨平台导入、导出单独笔记。
- 关联项目入口合并为单一按钮，仅在已有关系时显示关联内容。
- 建立"演示行记数据"和本地文件夹持久化机制。

### 设置与外观

- 将左下角入口整合为完整设置中心。
- 集成语言、常规、外观、网盘同步、开发者测试和关于页面。
- 添加自定义通知标题、内容和提醒时间。
- 添加浅色、深色、蓝黄、护眼及自定义主题。
- 支持字体、字号和自定义字体家族。
- 修复深色模式、字号应用不完整和弹窗透明问题。
- 增加简体中文、繁体中文、英语界面适配和国旗图标。

### 同步与界面

- OneDrive 采用本地同步文件夹方案。
- 显示已选择的同步文件夹。
- 添加自动同步和应用内同步状态通知。
- 修复列表重复动画、待办完成刷新动画和移动端布局。
- 更换网页图标及页面 LOGO。

## v0.1.0 Prototype（2026-07-16）

### 主要功能

- 完成 Electron 桌面端原型，支持 Windows、macOS 和 Linux。
- 实现笔记与待办融合的三栏布局：
  - 左侧：智能视图与文件夹管理
  - 中间：笔记、待办统一列表
  - 右侧：内容编辑区域
- 支持创建、搜索、筛选和删除笔记与待办。
- 笔记支持标题、富文本格式、列表、高亮和链接。
- 待办支持子任务、完成进度、截止日期、优先级、标签和补充说明。
- 支持中英文界面切换。
- 内容自动保存在本地。

### 网盘同步

- 桌面端可选择 OneDrive、Dropbox、坚果云等客户端的本地同步目录。
- 支持上传和下载 `acta-library.json` 资料库。
- Android 端增加系统文件夹选择器。
- Android 可向兼容 Storage Access Framework 的本地或网盘目录读写资料库。
- 已加入资料库格式与数据有效性检查。

### 界面与体验

- 使用现代纸张、墨色与低饱和配色。
- 加入非线性弹性动画、列表入场、编辑器切换和弹窗动效。
- 支持系统"减少动态效果"设置。
- 增加 Android 手机端响应式布局：
  - 底部导航
  - 中央悬浮新建按钮
  - 全屏滑入式编辑器
  - 返回按钮及安全区域适配

### Android 版本

- 使用 Capacitor 6 生成原生 Android 工程。
- 应用包名：`com.acta.xingji`

### 应用图标

- 使用提供的 `Acta.jpg` 作为应用图标。
- 提供可重复生成全部图标的 PowerShell 脚本。

### 工程与安全

- Electron 使用 Context Isolation 和安全的 Preload API。
- 添加内容安全策略 CSP。
- Android SDK、Gradle 和图标生成流程均已配置。
- 补充桌面端与 Android 构建说明。
